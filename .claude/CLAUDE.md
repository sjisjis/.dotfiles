# グローバル指示

## リモートへの反映は確認してから

- `git push`・PR作成など、リモートリポジトリへ反映する操作は実行前に必ず確認を取ること。ローカルでのコミットまでは進めてよい。
- リポジトリを分ける・新規作成するなどの構成変更も、実行前に必ず確認を取ること。
- ユーザーが明示的に「pushして」と指示した場合はそのまま実行してよい。

## やり取りの言語

- 確認・報告・説明は日本語を主体とする。
